# 📊 SCHÉMA WORKFLOW : SYSTÈME VOCAL / IA

## 🎤 WORKFLOW PRINCIPAL (VoiceRecorder.js)

```
┌─────────────────────────────────────────────────────────────────┐
│                    UTILISATEUR                                   │
│              Appuie sur "Enregistrer"                            │
└──────────────────────────┬──────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────────────┐
│  ÉTAPE 1 : ENREGISTREMENT VOCAL                                 │
│  ─────────────────────────────────────────────────────────────  │
│  • Audio.requestPermissionsAsync()                               │
│  • Audio.Recording (HIGH_QUALITY)                               │
│  • Format : M4A                                                  │
│  • Résultat : file://... (local)                                 │
└──────────────────────────┬──────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────────────┐
│  ÉTAPE 2 : UPLOAD SUPABASE STORAGE                              │
│  ─────────────────────────────────────────────────────────────  │
│  • Bucket : "voices"                                             │
│  • Nom : rec_{projectId}_{timestamp}.m4a                        │
│  • Résultat : Fichier dans Storage                               │
└──────────────────────────┬──────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────────────┐
│  ÉTAPE 3 : TRANSCRIPTION                                         │
│  ─────────────────────────────────────────────────────────────  │
│  ❌ whisper.rn (local) : CODE PRÉSENT MAIS NON UTILISÉ          │
│  ✅ OpenAI Whisper API : UTILISÉ                                 │
│                                                                  │
│  Service : transcriptionService.js                               │
│  • POST https://api.openai.com/v1/audio/transcriptions           │
│  • Headers : Authorization: Bearer {CLÉ API CLIENT} ⚠️          │
│  • Modèle : whisper-1                                            │
│  • Langue : fr                                                   │
│  • Résultat : Texte brut ("Il faut changer 3 prises...")          │
└──────────────────────────┬──────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────────────┐
│  ÉTAPE 4 : CORRECTION ORTHOGRAPHIQUE                            │
│  ─────────────────────────────────────────────────────────────  │
│  Service : transcriptionService.js                               │
│  • POST https://api.openai.com/v1/chat/completions              │
│  • Headers : Authorization: Bearer {CLÉ API CLIENT} ⚠️          │
│  • Modèle : gpt-4o-mini                                          │
│  • Prompt : Correcteur orthographique strict                     │
│  • Résultat : Texte corrigé ("Il faut changer 3 prises...")     │
└──────────────────────────┬──────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────────────┐
│  ÉTAPE 5 : ANALYSE DE LA NOTE                                   │
│  ─────────────────────────────────────────────────────────────  │
│  Service : quoteAnalysisService.js                               │
│  • POST https://api.openai.com/v1/chat/completions              │
│  • Headers : Authorization: Bearer {CLÉ API CLIENT} ⚠️          │
│  • Modèle : gpt-4o-mini                                          │
│  • Format : json_object                                          │
│  • Résultat : {                                                  │
│      type: "prestation",                                         │
│      categorie: "Électricité",                                   │
│      description: "Installation prises cuisine",                 │
│      quantite: 3,                                                │
│      unite: "pièce"                                              │
│    }                                                             │
└──────────────────────────┬──────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────────────┐
│  ÉTAPE 6 : SAUVEGARDE NOTE EN BASE                              │
│  ─────────────────────────────────────────────────────────────  │
│  Table : notes                                                   │
│  • project_id, client_id, user_id                                │
│  • type: "voice"                                                 │
│  • storage_path (chemin Storage)                                 │
│  • transcription (texte corrigé)                                  │
│  • analysis_data (JSON stringifié)                               │
└──────────────────────────┬──────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────────────┐
│  ÉTAPE 7 : GÉNÉRATION DEVIS (si prestation détectée)            │
│  ─────────────────────────────────────────────────────────────  │
│  Condition : analysis.type === "prestation"                      │
│                                                                  │
│  Service : ai_quote_generator.js                                │
│  • Parsing regex local (pas d'IA)                                │
│  • Base de prix hardcodée (PRICE_DATABASE)                       │
│  • Création devis + lignes                                       │
│  • Statut : "edition"                                            │
└─────────────────────────────────────────────────────────────────┘
```

---

## 🤖 WORKFLOW ALTERNATIF : DEVIS IA CONVERSATIONNEL

```
┌─────────────────────────────────────────────────────────────────┐
│                    UTILISATEUR                                   │
│         Clique "Générer devis IA"                                │
└──────────────────────────┬──────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────────────┐
│  COMPOSANT : DevisAIGenerator2.js                                │
│  ─────────────────────────────────────────────────────────────  │
│  • Récupère toutes les notes du chantier                         │
│  • Compile transcriptions                                        │
└──────────────────────────┬──────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────────────┐
│  SERVICE : aiConversationalService.js                            │
│  ─────────────────────────────────────────────────────────────  │
│  startDevisSession(transcription, projectId, clientId, userId)   │
│                                                                  │
│  POST /functions/v1/ai-devis-conversational                      │
│  Headers : Authorization: Bearer {SESSION_TOKEN} ✅              │
│  Body : {                                                        │
│    action: "start",                                              │
│    transcription: "...",                                         │
│    project_id: "...",                                            │
│    client_id: "..."                                              │
│  }                                                               │
└──────────────────────────┬──────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────────────┐
│  EDGE FUNCTION : ai-devis-conversational                         │
│  ─────────────────────────────────────────────────────────────  │
│  ✅ SÉCURISÉ : Clé API dans Deno.env.get("OPENAI_API_KEY")      │
│                                                                  │
│  • Appelle GPT-4o-mini avec clé serveur                          │
│  • Génère devis ou pose questions                                │
│  • Retourne : {                                                  │
│      status: "questions" | "ready",                              │
│      devis: {...},                                               │
│      questions: [...]                                            │
│    }                                                             │
└──────────────────────────┬──────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────────────┐
│  BOUCLE Q/R (max 3 tours)                                        │
│  ─────────────────────────────────────────────────────────────  │
│  Si questions → Utilisateur répond                               │
│  → answerQuestions(sessionId, reponses)                          │
│  → Edge Function traite → Nouvelles questions ou devis final     │
└──────────────────────────┬──────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────────────┐
│  CRÉATION DEVIS DÉFINITIF                                        │
│  ─────────────────────────────────────────────────────────────  │
│  createDevisFromAI(sessionId, devisData, projectId, clientId)    │
│  • Insert dans table "devis"                                     │
│  • Insert lignes dans "devis_lignes"                             │
│  • Statut : "edition"                                            │
└─────────────────────────────────────────────────────────────────┘
```

---

## 🔴 PROBLÈMES DE SÉCURITÉ IDENTIFIÉS

```
┌─────────────────────────────────────────────────────────────────┐
│  ❌ CLÉ API EXPOSÉE CÔTÉ CLIENT                                 │
│  ─────────────────────────────────────────────────────────────  │
│                                                                  │
│  Fichier : config/openai.js                                      │
│  Variable : EXPO_PUBLIC_OPENAI_API_KEY                           │
│                                                                  │
│  ⚠️ PROBLÈME :                                                   │
│  • Variable EXPO_PUBLIC_* = visible dans bundle JS               │
│  • Clé API accessible dans le code source                        │
│  • Risque de vol / abus                                          │
│                                                                  │
│  Utilisé dans :                                                  │
│  • transcriptionService.js (transcription + correction)          │
│  • quoteAnalysisService.js (analyse note)                        │
│                                                                  │
│  ✅ SOLUTION :                                                   │
│  • Migrer vers Edge Functions                                    │
│  • Clé API dans variables d'environnement serveur                │
└─────────────────────────────────────────────────────────────────┘
```

---

## ✅ WORKFLOW SÉCURISÉ (APRÈS MIGRATION)

```
┌─────────────────────────────────────────────────────────────────┐
│  CLIENT MOBILE                                                   │
│  ─────────────────────────────────────────────────────────────  │
│  VoiceRecorder.js                                                │
│  • Enregistre audio                                              │
│  • Upload Storage                                                │
│  • Appelle Edge Functions (pas d'API directe)                    │
└──────────────────────────┬──────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────────────┐
│  EDGE FUNCTION : transcribe-audio                                │
│  ─────────────────────────────────────────────────────────────  │
│  ✅ Clé API serveur (Deno.env.get("OPENAI_API_KEY"))             │
│  • Télécharge audio depuis Storage                               │
│  • Appelle Whisper API                                           │
│  • Retourne transcription                                         │
└──────────────────────────┬──────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────────────┐
│  EDGE FUNCTION : correct-text                                    │
│  ─────────────────────────────────────────────────────────────  │
│  ✅ Clé API serveur                                              │
│  • Appelle GPT-4o-mini                                            │
│  • Retourne texte corrigé                                         │
└──────────────────────────┬──────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────────────┐
│  EDGE FUNCTION : analyze-note                                    │
│  ─────────────────────────────────────────────────────────────  │
│  ✅ Clé API serveur                                              │
│  • Appelle GPT-4o-mini                                            │
│  • Retourne analyse JSON                                          │
└─────────────────────────────────────────────────────────────────┘
```

---

## 📊 COMPARAISON AVANT / APRÈS

| Aspect | AVANT (Actuel) | APRÈS (Sécurisé) |
|--------|----------------|------------------|
| **Clé API** | ❌ Côté client (`EXPO_PUBLIC_*`) | ✅ Côté serveur (Edge Function) |
| **Sécurité** | ❌ Exposée dans bundle JS | ✅ Protégée (variables env serveur) |
| **Rate Limiting** | ❌ Impossible | ✅ Possible (Edge Function) |
| **Logs** | ❌ Dispersés | ✅ Centralisés (Supabase) |
| **Coûts** | ❌ Non contrôlés | ✅ Contrôlables (logs serveur) |
| **Transcription locale** | 🟡 Code présent mais non utilisé | 🟡 Supprimé ou activé |

---

**Fin du schéma**

