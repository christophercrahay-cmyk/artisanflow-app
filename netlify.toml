[build]
  # Déploiement du HTML statique de web/sign (pas de build nécessaire)
  base = "."
  publish = "web/sign"
  command = "echo 'No build needed for static HTML'"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200


