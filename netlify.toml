[build]
  # On force le build depuis la racine pour éviter tout override UI.
  command = "npm install --prefix web/signature && npm run --prefix web/signature build"
  publish = "web/signature/dist"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200


